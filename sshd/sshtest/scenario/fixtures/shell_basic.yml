# Basic interactive shell test
name: interactive shell
description: Test basic shell interaction with echo command

steps:
  - client: test
    actions:
      - connect
      - shell
      - line: "echo shell-test"
    expect:
      - wait_for_output:
          text: "shell-test"
          timeout: 5s
      - event: shell.started
